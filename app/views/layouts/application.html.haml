<!DOCTYPE html>
%html
  %head
    %title Relaxotron
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    %script{type: "text/javascript", src: "//use.typekit.net/cnn7kco.js"}
    %script{type: "text/javascript"}
      try{Typekit.load();}catch(e){}

  %body
    .content
      #header
        .logo
          = link_to root_path do
            Relaxotron

      #flash
        - flash.each do |key, value|
          %div{:id => "flash#{key}"}
            = value
      .main
        = yield

      #footer
        = link_to "Articles", "#", class: "tk-trajan-sans-pro"
        = link_to "Some bullshit", "#", class: "tk-trajan-sans-pro"
        = link_to "Store", "#", class: "tk-trajan-sans-pro"
