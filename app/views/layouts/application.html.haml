<!DOCTYPE html>
%html
  %head
    %title Relaxotron
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    %script{type: "text/javascript", src: "//use.typekit.net/cnn7kco.js"}
    %script{type: "text/javascript"}
      try{Typekit.load();}catch(e){}

  %body.forrest
    .content
      #header.fadeable
        .logo
          = link_to root_path do
            Placidity

      #flash
        - flash.each do |key, value|
          %div{:id => "flash#{key}"}
            = value
      .main
        = yield

      #footer.fadeable
        = link_to "Articles", "#", class: "tk-vinter-web"
        = link_to "Some bullshit", "#", class: "tk-vinter-web"
        = link_to "Store", "#", class: "tk-vinter-web"
